# unsupported-version

Malformed fixture proving unsupported-version handling. It intentionally marks
`meta.json` as `spec_version: "0.1"`; pre-0.2 traces do not have full backwards
compatibility support.
