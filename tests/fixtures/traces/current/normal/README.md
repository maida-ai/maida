# normal

Proves the happy-path current `spec_version: "0.2"` storage contract:
one completed root span, one LLM call, one tool call, deterministic token
usage, deterministic tool args/results, and a synthetic `RUN_END` projection.
