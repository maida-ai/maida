# tool-call-spike

Proves a completed run with a deterministic spike in tool calls. It is used
against the `normal` baseline to prove diff and assertion behavior for added
tools and excessive tool-call count.
